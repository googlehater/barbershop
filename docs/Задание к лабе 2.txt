Лабораторная работа: Серверный рендеринг и работа с БД

Тема: Реализация проекта с серверным рендерингом страниц, работа с базой данных и безопасностью.
Предметная область: берите тему и страницы с прошлой лабораторной. Если по какой то причине взять ее не получится, пишите на почту

Цель лабораторной работы
 - Освоить серверный рендеринг HTML-страниц
 - Научиться безопасно работать с базой данных через чистый SQL
 - Использовать архитектуру MVC и принципы DI
 - Освоить контейнеризацию с Docker для приложения и БД
 - Реализовать CRUD операции и взаимосвязь таблиц

Требования к проекту
 - Все HTML-страницы должны формироваться на сервере. Клиентский JavaScript разрешён только для интерактивности
 - Развертывание БД и приложения через Docker Compose
 - Использование MVC архитектуры, контроллеры обрабатывают запросы, модели — работу с БД, представления — шаблоны HTML
 - CRUD операции минимум для одной сущности
 - Минимум три таблицы по предметной области. Не менее одной связи между таблицами (один-ко-многим или многие-ко-многим). Если нет идеи для предметной области — сделать таблицы для статистики действий/логов/переходов. Все таблицы должны быть в нормальных формах
 - Все запросы выполняются только через чистый SQL, без ORM
 - Защита от SQL-инъекций обязательна (использование параметризованных запросов)
 - Обязательная защита от XSS
 - Проверка и фильтрация входных данных
 - Dependency Injection. Использовать DI для подключения к БД, логгера, сервисов и т.д.
 - Все параметры (строки подключения к БД, ключи) хранятся в переменных окружения или конфигурационных файлах

Вопросы по теории:
Базы данных:
 - Виды связей (один-ко-многим, многие-ко-многим, один-к-одному)
 - Типы полей в БД (INT, VARCHAR, TEXT, DATE, BOOLEAN и т.д.)
 - ID и первичные ключи
 - Транзакции и асинхронность:
 - Что такое транзакции и зачем они нужны
 - Преимущества асинхронного доступа к БД
 - Как реализовать связи один-ко-многим и многие-ко-многим в SQL
 - Что делает JOIN и для чего он нужен
 - Нормализация в БД
 - Индексы
Серверный рендеринг и шаблонизация:
 - Преимущества и недостатки серверного рендеринга
 - Разница между SSR и SPA
 - MVC и DI:
Docker:
 - Преимущества контейнеризации приложения и БД
 SQL-инъекции и XSS:
 - Что такое SQL-инъекция и как от неё защищаться
 - Что такое XSS и как защитить приложение
 CRUD операции:
 - Что такое CRUD и как реализовать каждую операцию на сервере
 - Связи в таблицах:

В подготовке к лабе может помочь(если прочитаете или используете хоть что то оттуда, буду рад):
https://habr.com/ru/articles/181772/ - MVC
https://habr.com/ru/companies/tensor/articles/779698/ - основы SQL
https://habr.com/ru/articles/745948/ - шпора по SQL
https://habr.com/ru/articles/798585/ - SSR
https://habr.com/ru/sandbox/235584/ - SSR
https://habr.com/ru/companies/netologyru/articles/967546/ - докер
https://habr.com/ru/companies/alfa/articles/717896/ - XSS атака
https://habr.com/ru/articles/725134/ - SQL инъекции
https://habr.com/ru/articles/254773/ - нормальные формы БД
https://habr.com/ru/companies/jugru/articles/446562/ - асинхронность(много и сложно)